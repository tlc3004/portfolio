import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "Asistente IA",
  initialMessages: [createChatBotMessage("Hola Soy el asistente personal de Antonio ,te contare todo sobre el .")],
};

export default config;
